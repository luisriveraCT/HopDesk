# Claude Code Instructions — HopDesk Items Wiring Fix

This document is a sequence of atomic tasks. Each task is small, file-and-line specific, and has a clear acceptance criterion. Execute in order — later tasks may depend on earlier ones.

**Conventions:**
- File paths are relative to the project root (`Antiguedad_App/`).
- "Verify by running the app" means: launch the Shiny app, perform the listed user action, and confirm the listed expected outcome.
- All edits should preserve existing comments and code style.
- Do **not** add author/date headers, "Generated by" comments, or anything that wasn't there before.

---

## Phase 0 — Cleanup of dead code

These don't change behavior, but they make subsequent edits safer because there are fewer red herrings.

### Task 0.1 — Delete `app_timed.R`
**Action:** Delete `app_timed.R` from the project root.
**Reason:** Diagnostic timer-instrumented copy of `app.R`. Not sourced from production. Confusing.
**Verify:** App launches normally with `shiny::runApp()`. No errors mentioning `app_timed`.

### Task 0.2 — Delete root `global.R`
**Action:** Delete `/global.R` (in the project root, NOT `R/global.R`).
**Reason:** Stale duplicate. `R/global.R` is the live one and supersedes it. Shiny auto-sources both; the second wins, but the first loads stale `COMPANY_MAP` first and pollutes the global env unnecessarily.
**Verify:** App launches. `COMPANY_MAP` includes `PL = "Paragon Logistics"`. No errors.

### Task 0.3 — Delete `R/bootstrap_proveedores.R`
**Action:** Delete the file `R/bootstrap_proveedores.R`.
**Reason:** One-shot historical migration helper. `grep -rn "bootstrap_proveedores" R/ app.R` confirms zero references outside the file itself.
**Verify:** App launches without `object 'bootstrap_proveedores' not found` errors.

### Task 0.4 — Remove unused `manual_entry_handlers()` function
**File:** `R/manual_entry_handlers.R`
**Action:** Delete the function `manual_entry_handlers <- function(...)` (lines 149–173). Keep `manual_entry_tab_content` and `manual_edit_handlers` — they're still used.
**Reason:** Replaced by `show_combined_entry_modal` in `staging_browse_module.R`. Not called anywhere.
**Verify:** `grep -n "manual_entry_handlers\b" R/ app.R` returns only the file itself with the docstring at the top. App's "Agregar" flow still works.

---

## Phase 1 — Harmonize the `Codigo` column

This is the highest-leverage edit in the whole plan. Done correctly, downstream `keep` lists and slicing operations pick up Codigo for SAP rows automatically.

### Task 1.1 — Add unified `Codigo` column in `build_ledger_df`
**File:** `R/data_pipeline.R`
**Function:** `build_ledger_df` (starts at line 159)
**Action:**
After the `bind_rows(df, manual_sub)` at line 192, but before `if (!nrow(df)) return(NULL)` at line 196, insert a column-harmonization step:

```r
  # Harmonize Codigo across SAP and manual rows. SAP carries CardCode under
  # `Código de proveedor` (AP) or `Código de cliente` (AR); manual carries it
  # as `Codigo`. We coalesce into a single canonical `Codigo` column so every
  # downstream slice (keep lists, .fresh_lu, vencidos disp_df, search keep)
  # picks it up uniformly.
  if (!"Codigo" %in% names(df)) df[["Codigo"]] <- NA_character_
  if ("Código de proveedor" %in% names(df)) {
    df[["Codigo"]] <- dplyr::coalesce(
      as.character(df[["Codigo"]]),
      as.character(df[["Código de proveedor"]])
    )
  }
  if ("Código de cliente" %in% names(df)) {
    df[["Codigo"]] <- dplyr::coalesce(
      as.character(df[["Codigo"]]),
      as.character(df[["Código de cliente"]])
    )
  }
  df[["Codigo"]] <- trimws(df[["Codigo"]] %||% "")
  df[["Codigo"]][df[["Codigo"]] == ""] <- NA_character_
```

**Acceptance:** After this change, `df_combined()` always has a non-NA `Codigo` for SAP rows (assuming SAP returned a CardCode) and the manual entry's value for manual rows.

**Verify:**
1. Launch app, log in, wait for SAP load.
2. Click any day on the AR or AP calendar to open the day modal.
3. The day modal should still render with the same totals as before.
4. (Optional dev verification) — in R console: `df_combined()` from inside any ledger module should now have `Codigo` populated for SAP rows.

### Task 1.2 — Calendar day modal: keep Codigo through staging (file 1 of 4)
**File:** `R/ledger_module.R`
**Function:** `.fresh_lu` (line 838)
**Action:** Add `"Codigo"` to the column slice on line 849:

```r
        lu <- unique(df_now[key_now %in% key_inv,
               c("Empresa","Moneda","Documento","Parte","Codigo","Importe","FechaEff"), drop = FALSE])
```

Also update the `fallback_lu` callers — `stage_all` at line 558, `stage_sel` at line 604, `cart_grp_click` at line 735, `cart_inv_click` at line 795 — so each fallback `unique(detail[, c(...)])` includes `"Codigo"`. Specifically:

- Line 558:
  ```r
        unique(d[, c("Empresa","Moneda","Documento","Parte","Codigo","Importe","FechaEff"), drop = FALSE]))
  ```
- Line 604:
  ```r
        unique(detail[, c("Empresa","Moneda","Documento","Parte","Codigo","Importe","FechaEff"), drop = FALSE]))
  ```
- Line 735:
  ```r
            unique(detail[, c("Empresa","Moneda","Documento","Parte","Codigo","Importe","FechaEff"), drop=FALSE]))
  ```
- Line 795:
  ```r
      inv_rows <- unique(detail[inv_mask,
        c("Empresa","Moneda","Documento","Parte","Codigo","Importe","FechaEff"), drop=FALSE])
  ```

**Note:** Task 1.1 added `Codigo` to `df_combined`. Task 0.x will have caused `detail` (built at lines 511–517) to already include Codigo because line 514's `keep` list already includes `"Codigo"` — that line catches Codigo for both SAP and manual rows now that Task 1.1 unified it. So no edit to lines 511–517 is needed.

**Acceptance:** `new_rows` produced by `stage_all`, `stage_sel`, `cart_grp_click`, `cart_inv_click` all carry `Codigo`.

### Task 1.3 — Calendar day modal: include Codigo in `new_rows` writes
**File:** `R/ledger_module.R`
**Action:** In each of the four staging blocks, add `Codigo` to the `new_rows` schema and to the final column-order slice. Specifically:

- `stage_all`, lines 567–568:
  ```r
      new_rows <- new_rows[, c("id","ledger","Empresa","Moneda","Documento",
                                "Parte","Codigo","Importe","FechaVenc","staged_by","staged_at","status"), drop = FALSE]
  ```
- `stage_sel`, lines 613–614:
  ```r
      new_rows <- new_rows[, c("id","ledger","Empresa","Moneda","Documento",
                                "Parte","Codigo","Importe","FechaVenc","staged_by","staged_at","status"), drop = FALSE]
  ```
- `cart_grp_click`, lines 744–745:
  ```r
        new_rows <- new_rows[, c("id","ledger","Empresa","Moneda","Documento",
                                  "Parte","Codigo","Importe","FechaVenc","staged_by","staged_at","status"), drop = FALSE]
  ```
- `cart_inv_click` block at lines 812–826: rewrite `new_row` to include `Codigo`:
  ```r
      one <- inv_rows[j, , drop=FALSE]
      new_row <- data.frame(
        id         = uuid::UUIDgenerate(),
        ledger     = ledger,
        Empresa    = one[["Empresa"]],
        Moneda     = one[["Moneda"]],
        Documento  = one[["Documento"]],
        Parte      = one[["Parte"]],
        Codigo     = one[["Codigo"]] %||% NA_character_,
        Importe    = one[["Importe"]],
        FechaVenc  = as.Date(one[["FechaEff"]]),
        staged_by  = shared$current_user(),
        staged_at  = Sys.time(),
        status     = "pending",
        stringsAsFactors = FALSE
      )
  ```

**ALSO — Remove the dead `tipo` column.** In all four blocks, delete the line `new_rows[["tipo"]] <- if (ledger == "AR") "cobro" else "pago"` (and its variant in `cart_inv_click`). Remove `"tipo"` from the column-order slices too. Reason: `tipo` is not in `pagar_hoy` schema and is silently dropped by `.normalize` on save.

**Acceptance:** All four staging buttons produce `new_rows` with columns `id, ledger, Empresa, Moneda, Documento, Parte, Codigo, Importe, FechaVenc, staged_by, staged_at, status` — no `tipo`.

**Verify:** Stage an SAP invoice from a calendar day modal → check the row in `pagar_hoy_db` has `Codigo` populated, no `tipo` column.

### Task 1.4 — Vencidos: thread Codigo end-to-end (DOM → JS → R)
**Files:** `R/vencidos_module.R`, `www/vencidos.js`, `R/search_module.R`

**Edit A — DOM template:** `R/vencidos_module.R`, in `make_tr` (the row builder at lines 468–487), add a `data-codigo` attribute. Insert **after** the `data-invid` attribute (between current line 484 and 485):

```r
            ' data-codigo="',   htmltools::htmlEscape(row[["Codigo"]] %||% ""), '"',
```

The `Codigo` column is now available on `row` because Task 1.1 unified it. Verify by checking the upstream df construction in `vencidos_module.R::vencidos_df` reactive (~line 220) — `df` comes from `build_ledger_df` and `disp_df` is built at line 359. **Add `Codigo` to the `disp_df` builder:**

```r
        disp_df <- data.frame(
          # ... existing fields ...
          Codigo     = if ("Codigo" %in% names(df)) df[["Codigo"]] %||% "" else "",
          # ... rest ...
        )
```

Place it as a new field in the `data.frame(...)` call between `Documento` and `Referencia` for readability.

**Edit B — JS payload:** `www/vencidos.js`, in `venRowPayload` (lines 20–33), add `codigo`:

```js
  function venRowPayload(r) {
    return {
      ledger   : r.dataset.ledger,
      empresa  : r.dataset.empresa,
      moneda   : r.dataset.moneda,
      documento: r.dataset.documento,
      source   : r.dataset.source,
      inv_id   : r.dataset.invid    || '',
      parte    : r.dataset.parteraw || r.dataset.parte,
      codigo   : r.dataset.codigo   || '',
      importe  : parseFloat(r.dataset.importe),
      fecha    : r.dataset.fecha,
      tipo     : r.dataset.tipo
    };
  }
```

**Edit C — R handler:** `R/search_module.R`, function `handle_invoice_action` (line 692), `keys_df` builder at lines 699–711:

```r
  keys_df <- data.frame(
    ledger    = vapply(rows, `[[`, character(1), "ledger"),
    Empresa   = vapply(rows, `[[`, character(1), "empresa"),
    Moneda    = vapply(rows, `[[`, character(1), "moneda"),
    Documento = vapply(rows, `[[`, character(1), "documento"),
    source    = vapply(rows, `[[`, character(1), "source"),
    inv_id    = vapply(rows, `[[`, character(1), "inv_id"),
    Parte     = vapply(rows, `[[`, character(1), "parte"),
    Codigo    = vapply(rows, function(r) r[["codigo"]] %||% "", character(1)),
    Importe   = vapply(rows, `[[`, numeric(1),   "importe"),
    Fecha     = vapply(rows, `[[`, character(1), "fecha"),
    tipo      = vapply(rows, function(r) r[["tipo"]] %||% "", character(1)),
    stringsAsFactors = FALSE
  )
```

**Edit D — `new_rows` build:** `R/search_module.R`, in the `stage_all`/`stage_selected` branch at lines 833–849:

```r
    new_rows <- data.frame(
      id        = vapply(seq_len(nrow(keys_df)), function(x) uuid::UUIDgenerate(), character(1)),
      ledger    = keys_df$ledger,
      Empresa   = keys_df$Empresa,
      Moneda    = keys_df$Moneda,
      Documento = keys_df$Documento,
      Parte     = keys_df$Parte,
      Codigo    = trimws(keys_df$Codigo %||% ""),
      Importe   = keys_df$Importe,
      FechaVenc = as.Date(vapply(keys_df$Fecha, parse_fecha, numeric(1)),
                          origin = "1970-01-01"),
      staged_by = current_user,
      staged_at = Sys.time(),
      status    = "pending",
      stringsAsFactors = FALSE
    )
```

(Drop the `tipo = ifelse(...)` line entirely — `tipo` is dead weight per Task 1.3.)

**Acceptance:** Vencidos green-button stage → row in `pagar_hoy_db` has `Codigo` populated.

### Task 1.5 — Búsqueda: thread Codigo end-to-end (parallel to 1.4)
**File:** `R/search_module.R`

**Edit A — DOM template:** at the row builder around lines 365–396, add `data-codigo`:

Insert after the `data-invid` attribute (around line 379):
```r
                ' data-codigo="',   htmltools::htmlEscape(row[["Codigo"]] %||% ""),  '"',
```

You'll need `Codigo` available in `row`. Look at the upstream `build_ledger_table` at lines 25–78. The `keep` filter at lines 71–76 needs `"Codigo"` added:

```r
    keep <- intersect(
      c("Ledger","Tipo","Empresa","Fecha","Parte","Documento","Factura",
        "Moneda","Importe","Etiqueta","source","id","Codigo",
        "FechaVenc_Original","FechaVenc_Proyectada"),
      names(df)
    )
```

(Codigo is now in `df` thanks to Task 1.1.)

**Edit B — JS payloads:** at lines 461–480 (`getCheckedRows`) and 482–501 (`getAllVisibleRows`), add `codigo`:

```js
                out.push({
                  ledger   : r.dataset.ledger,
                  empresa  : r.dataset.empresa,
                  moneda   : r.dataset.moneda,
                  documento: r.dataset.documento,
                  source   : r.dataset.source,
                  inv_id   : r.dataset.invid,
                  parte    : r.dataset.parte,
                  codigo   : r.dataset.codigo || '',
                  importe  : parseFloat(r.dataset.importe),
                  fecha    : r.dataset.fecha,
                  tipo     : r.dataset.tipo
                });
```

**Edit C, D:** No additional R-side edits — Task 1.4's edits to `handle_invoice_action` cover both Búsqueda and Vencidos because they share the handler.

**Acceptance:** Búsqueda's "Agregar todos / selección" → rows in `pagar_hoy_db` have `Codigo` populated.

### Task 1.6 — Bancos return-to-calendar: keep Codigo
**File:** `R/bancos_module.R`
**Function/Block:** around line 2783 — `new_ph_row` constructor.
**Action:** Add `Codigo`:

```r
      new_ph_row <- tibble::tibble(
        id        = uuid::UUIDgenerate(),
        ledger    = ph_ledger,
        Empresa   = as.character(row$empresa),
        Moneda    = as.character(row$moneda),
        Documento = as.character(row$documento),
        Parte     = as.character(row$parte),
        Codigo    = trimws(as.character(row$codigo %||% "")),
        Importe   = as.numeric(row$importe),
        FechaVenc = as.Date(row$fecha),
        staged_by = shared$current_user(),
        staged_at = Sys.time(),
        status    = "pending"
      )
```

(Drop the `tipo` field if present.)

**Note:** `row$codigo` may not exist on the bancos confirmados/papelera row schema. If after this change the field is always `""`, **also** add `codigo` (lowercase) to `.schema_bancos_confirmados` in `R/bancos_persistence.R:55–69`, and update bancos confirmation handlers to capture it from `pagar_hoy_db` before moving the row to confirmed. Specifically: in `R/pagar_hoy_module.R` look for the section that writes to `bancos_confirmados` (around lines 939–1000, the "Confirm AP" handler) and ensure `codigo = row$Codigo` is included in the inserted record.

**Acceptance:** Restoring a confirmed payment back to the calendar → the resulting Agenda row has `Codigo`.

### Task 1.7 — Intercompany: keep Codigo + use upsert_pagar_hoy
**File:** `R/interco_module.R`
**Function:** `.ic_send_rows` at lines 326–366.
**Action:** Two edits in one:

1. Add `Codigo` to the row builder (around lines 347–360). The IC table data comes from SAP snapshots (`R/interco_module.R::.load_ic_data` at line 87+) which carry the original `Código de proveedor`/`Código de cliente`. Plumb that through:

   At line 347:
   ```r
       new_rows <- dplyr::bind_rows(lapply(seq_len(nrow(rows)), function(i) {
         r   <- rows[i, ]
         doc <- tryCatch(as.character(r[["Documento"]][1]),
                         error = function(e) tryCatch(as.character(r[["Factura"]][1]),
                                                      error = function(e) ""))
         mon <- tryCatch(as.character(r[["Moneda"]][1]), error = function(e) "MXN")
         # The IC table data comes from raw SAP snapshots, so the SAP-named codigo
         # column is still on `r` and not yet harmonized. Read either name.
         codigo_val <- tryCatch({
           cc <- if ("Codigo" %in% names(r)) r[["Codigo"]][1]
                 else if ("Código de proveedor" %in% names(r)) r[["Código de proveedor"]][1]
                 else if ("Código de cliente" %in% names(r)) r[["Código de cliente"]][1]
                 else ""
           trimws(as.character(cc %||% ""))
         }, error = function(e) "")
         tibble::tibble(
           id        = paste0("IC_", pair$ini, "_", doc, "_", as.integer(Sys.time()) + i),
           ledger    = pair$ledger,
           Empresa   = pair$empresa,
           Moneda    = mon,
           Documento = doc,
           Parte     = pair$counterpart,
           Codigo    = codigo_val,
           Importe   = as.numeric(r[[amt_col]][1] %||% 0),
           FechaVenc = tryCatch(as.Date(r[[due_col]][1]), error = function(e) Sys.Date()),
           staged_by = user_id,
           staged_at = Sys.time(),
           status    = "pending"
         )
       }))
   ```

2. Replace the `bind_rows + distinct(id)` at line 362 with `upsert_pagar_hoy`:
   ```r
       combined <- upsert_pagar_hoy(ph, new_rows)
       shared$pagar_hoy_db(combined)
       tryCatch(save_pagar_hoy(combined), error = function(e) NULL)
       nrow(new_rows)
   ```

**Acceptance:** IC "Agregar a Agenda" → rows in `pagar_hoy_db` have `Codigo`. Same invoice clicked twice doesn't duplicate.

### Task 1.8 — Treasury map: keep Codigo + use upsert_pagar_hoy
**File:** `R/treasury_map_module.R`
**Block:** lines 812–846, `observeEvent(input$send_to_agenda)`.
**Action:** Two edits parallel to Task 1.7.

1. The `payment_plan` items at line 824 don't currently carry a Codigo. Look at where plan items are added — likely an observer named something like `add_to_plan` or in the `payment_plan(...)` calls earlier in the file. Find every place that builds the plan list and ensure each plan element carries a `Codigo` field (read from the `Codigo` column in the source df, which is harmonized after Task 1.1).

   For the staging itself at lines 824–838:
   ```r
       new_rows <- dplyr::bind_rows(lapply(plan, function(x) {
         tibble::tibble(
           id        = paste0("IC_", x$from_ini, "_", x$Documento, "_", as.integer(Sys.time())),
           ledger    = "AP",
           Empresa   = x$from_full,
           Moneda    = x$Moneda,
           Documento = x$Documento,
           Parte     = x$to_full,
           Codigo    = trimws(as.character(x$Codigo %||% "")),
           Importe   = x$Importe %||% 0,
           FechaVenc = tryCatch(as.Date(x$FechaVenc), error = function(e) Sys.Date()),
           staged_by = user_id,
           staged_at = Sys.time(),
           status    = "pending"
         )
       }))
   ```

2. Replace the `bind_rows + distinct(id)` at line 840:
   ```r
       combined <- upsert_pagar_hoy(ph, new_rows)
   ```

**Acceptance:** Treasury map "Send to agenda" → rows have `Codigo`, no duplicates from re-clicking.

### Task 1.9 — Add `Codigo` to manual_inv editing path
**File:** `R/ledger_module.R`
**Block:** Around line 1084 where `manual_edit_handlers` is called — verify it's already passing through Codigo. Look at the `existing_row` populated for the editor. Manual entries already store Codigo, but check that when the user edits an existing manual entry and re-saves, Codigo round-trips correctly.

**Action:** No code edit unless verification fails. Just confirm:
- `manual_edit_handlers` (in `R/manual_entry_handlers.R:72`) reads `existing_row$Codigo` and pre-fills `me_codigo` (line 121). ✅
- `app.R:1370` and `app.R:1408` write `Codigo = trimws(input$me_codigo %||% "")`. ✅

If anything's not as documented, fix it minimally.

---

## Phase 2 — Fix the manual-entry-disappears bug

### Task 2.1 — Fix `bancos_confirmados` match key in `df_combined`
**File:** `R/ledger_module.R`
**Block:** lines 110–123 (Source 2 confirmation matching).
**Action:** Add `Moneda` to the match key, parallel to Source 1 at lines 100–108. Replace lines 115–122 with:

```r
        if (nrow(conf_active)) {
          bc_keys <- unique(conf_active[, c("empresa","documento","moneda"),
                                        drop = FALSE])
          # bancos_confirmados uses lowercase column names — normalize to match df.
          # Include Moneda so a USD manual entry is not confused with a same-Documento
          # MXN bancos_confirmados row (or vice versa).
          match_key <- paste(df[["Empresa"]], df[["Documento"]], df[["Moneda"]])
          conf_key  <- paste(bc_keys[["empresa"]], bc_keys[["documento"]], bc_keys[["moneda"]])
          df[["confirmed"]] <- df[["confirmed"]] | (match_key %in% conf_key)
        }
```

**Acceptance:** A manual entry for `(NTS, USD, ABC-123)` is **not** marked confirmed when bancos_confirmados has a row for `(NTS, MXN, ABC-123)`. Verify by:

1. In bancos, confirm any payment for empresa NTS, currency MXN, document ABC-999.
2. Create a manual entry for the same NTS, but currency USD, document ABC-999.
3. Save with "Agregar a Agenda" toggle off — entry must appear on the calendar normally.
4. Save with toggle on — entry appears in Agenda AND on the calendar.

### Task 2.2 — Defensive: don't silently show all on empresa filter mismatch
**File:** `R/ledger_module.R`
**Block:** lines 76–87.
**Action:** Replace the silent-fallback with an explicit empty result + warning:

```r
      if (length(emp) && "Empresa" %in% names(df)) {
        df_filtered <- dplyr::filter(df, Empresa %in% emp)
        if (nrow(df_filtered) == 0 && nrow(df) > 0) {
          # Filter eliminates everything — likely a stale-snapshot Empresa name
          # mismatch. Surface this clearly rather than showing all data.
          warning("[DF_COMBINED] empresa filter ", paste(emp, collapse=","),
                  " matched 0 rows of ", nrow(df), " — check empresas.rds vs snapshot")
          showNotification(
            "El filtro de Empresa no coincide con los datos cargados. Verifica la lista de empresas.",
            type = "warning", duration = 5)
        }
        df <- df_filtered
      }
```

**Acceptance:** With an intentional empresa name mismatch, the calendar shows empty (not all data) and a warning notification appears.

### Task 2.3 — Preserve `empresa_sel_rv` user choice across `empresas_db()` updates
**File:** `app.R`
**Block:** lines 643–649.
**Action:** Don't blindly reset to all empresas. Keep the existing user filter; only ADD newly-active empresas to the selection (so a freshly-added PL gets selected by default but the user's existing filter is preserved).

```r
  observeEvent(empresas_db(), {
    cmap     <- company_map_rv()
    cur_sel  <- empresa_sel_rv()
    all_now  <- unname(cmap)
    # Keep whatever the user had selected, intersected with the current valid list,
    # plus any brand-new empresas the user hasn't seen yet.
    prev <- isolate(.GlobalEnv$.prev_company_map %||% character(0))
    new_empresas <- setdiff(all_now, prev)
    keep_sel     <- intersect(cur_sel, all_now)
    new_sel      <- if (length(keep_sel)) union(keep_sel, new_empresas) else all_now
    empresa_sel_rv(new_sel)
    assign(".prev_company_map", all_now, envir = .GlobalEnv)
    assign("COMPANY_MAP", cmap, envir = .GlobalEnv)
  }, ignoreNULL = TRUE)
```

**Acceptance:** With NTS toggled solo as filter, adding PL via Empresas → `empresa_sel_rv` becomes `c("Networks Trucking Services", "Paragon Logistics")` (NTS preserved + PL auto-added), not the full list of all empresas.

---

## Phase 3 — Make company list reactive everywhere

The goal: adding a new empresa via the Empresas panel should make it visible in every UI surface within the same session, without app restart.

### Task 3.1 — `pagar_hoy_module` reactive empresa tabs
**File:** `R/pagar_hoy_module.R`

**Problem:** `pagarHoyUI` (line 18) and `pagarHoyServer` (line 121) both capture `unname(COMPANY_MAP)` once at session start. New empresas added mid-session don't get tabs.

**Action — UI side:** Convert the static panels list to a server-driven `uiOutput`. Replace the `tagList(...)` body of `pagarHoyUI` with the existing JS scripts plus a single `uiOutput(ns("ph_panels_ui"))`. Move the `do.call(tabsetPanel, ...)` logic into a `renderUI` in the server function that depends on `shared$company_map()`.

```r
pagarHoyUI <- function(id) {
  ns <- NS(id)
  tagList(
    tags$script(HTML(sprintf("
      // ... existing JS handlers (off/on click.phlookup, etc.) ...
    ", paste0(id, "-ph_lookup_parte"),
       paste0(id, "-ph_link_supplier"),
       paste0(id, "-ph_unlink_supplier"),
       paste0(id, "-ph_cycle_alias")
    ))),
    div(
      class = "ph-container d-flex flex-column h-100",
      div(class = "ph-summary-bar d-flex align-items-center gap-3 px-3 py-2 border-bottom bg-white",
        # ... existing summary bar (Fecha, global_total_ui, load_bancos_saldos, download_all, clear_all) ...
      ),
      uiOutput(ns("ph_panels_ui"))   # ← server builds this reactively
    )
  )
}
```

**Action — Server side:** Inside `pagarHoyServer`, add at the top (after the existing top-of-function lines):

```r
    # Reactive company list — sourced from shared$company_map() so adding a new
    # empresa via the Empresas panel updates the tabs without app restart.
    all_companies_rv <- reactive({
      cmap <- shared$company_map()
      sort(unname(cmap))
    })
```

Replace ALL static reads of `all_companies` (line 124 and downstream) with `all_companies_rv()`. Specifically:

- Line 124: delete (we have the reactive now).
- Line 128 (saldos_apertura init): convert to a reactive that updates when companies change. This is more invasive — convert `saldos_apertura` from a flat reactiveVal to a reactiveVal whose default expands when new empresas appear:
  ```r
      saldos_apertura <- reactiveVal(setNames(list(), character()))

      # Ensure every current company has a balance slot. Runs whenever
      # shared$company_map() changes.
      observe({
        cur <- saldos_apertura()
        for (e in all_companies_rv()) {
          if (is.null(cur[[e]])) cur[[e]] <- list(MXN = 0, USD = 0)
        }
        saldos_apertura(cur)
      })
  ```

- Lines 377–383, 414–420, 424+ (`lapply(all_companies, ...)` blocks): wrap each in a reactive context that depends on `all_companies_rv()`. Or, more pragmatically, leave the `lapply` but use `all_companies_rv()` instead of `all_companies`.

- Build the panels in a `renderUI`:
  ```r
      output$ph_panels_ui <- renderUI({
        all_cos <- all_companies_rv()
        panels <- lapply(all_cos, function(emp) {
          tabPanel(
            title = emp,
            value = emp,
            div(class = "ph-company-panel p-3",
              # ... rest unchanged (same content as the original UI panels) ...
            )
          )
        })
        do.call(tabsetPanel, c(panels, list(id = ns("company_tab"))))
      })
  ```

**Caveat:** The per-empresa observers (`observeEvent(input[[paste0("bal_cur_", emp)]], ...)`, `output[[paste0("calc_", emp)]] <- renderUI(...)`, etc.) are registered inside the `lapply(all_companies, ...)` block at lines 424+. **Per-input observers can't be easily removed in Shiny.** The cleanest approach: have the loop run inside an `observe()` that depends on `all_companies_rv()`, using a guard `session$userData[[paste0("ph_obs_", emp)]] <- TRUE` to register observers exactly once per empresa. Pseudocode:

```r
    observe({
      for (emp in all_companies_rv()) {
        if (isTRUE(session$userData[[paste0("ph_obs_", emp)]])) next
        session$userData[[paste0("ph_obs_", emp)]] <- TRUE

        local({
          emp_local <- emp   # capture per iteration
          # ... all the per-empresa observers and outputs go here, using `emp_local` ...
        })
      }
    })
```

**Acceptance:** Open Agenda de Hoy. In another tab, go to Empresas, add a brand-new test empresa "TEST" with initials "TST". Switch back to Agenda de Hoy — within a few seconds, a "TST" tab appears with the empty layout. Verify by clicking it.

**Notes for Claude Code:** This is the most invasive task. Be careful with iteration variable capture (`local({ emp_local <- emp; ... })`). Test by running the existing test cases for Agenda de Hoy after the conversion to confirm no regression for already-existing companies.

### Task 3.2 — `treasury_map_module` reactive empresa list
**File:** `R/treasury_map_module.R`

**Action:** Convert static reads to reactive `shared$company_map()`.

- Line 14 (`inv_map`) → make it a reactive:
  ```r
      inv_map_rv <- reactive({
        cmap <- shared$company_map()
        setNames(names(cmap), unname(cmap))
      })
  ```
  Replace all subsequent uses of `inv_map` with `inv_map_rv()`.

- Lines 32–33 (saldo init): convert to reactive expansion (parallel to Task 3.1's approach for `saldos_apertura`).

- Lines 51–52 (resolve initials from raw): use `cmap <- shared$company_map()` inline.

- Line 342 (`selectInput` choices): wrap the `selectInput` in a `renderUI`/`uiOutput` if it isn't already, so choices update when company_map changes. Or use `updateSelectInput` in an `observe({ shared$company_map(); ... })`.

- Lines 439, 444, 559, 579, 643, 644, 757, 758, 853 (server-side reads of COMPANY_MAP): replace each with `cmap <- shared$company_map()` at the top of the relevant observer/reactive, and use `cmap[[ini]]` / `names(cmap)` instead of `COMPANY_MAP[[ini]]` / `names(COMPANY_MAP)`.

**Acceptance:** Adding a new empresa via Empresas → it appears in the treasury map's target dropdown and as a node in the network diagram, without app restart.

### Task 3.3 — `interco_module` reactive empresa list
**File:** `R/interco_module.R`

**Action:**
- Line 45 (`inv_map`): convert to reactive (same pattern as Task 3.2).
- Lines 95–101 (`ic_codes` build inside `.load_ic_data`): replace `names(COMPANY_MAP)` with `names(shared$company_map())` (read inside the function so it's fresh on each call).

**Acceptance:** Adding a new empresa → IC table shows it (with empty rows until IC codes are configured).

### Task 3.4 — `proveedores_module` reactive empresa choices
**File:** `R/proveedores_module.R`

**Action:**
- Line 87 (Excel upload empresa filter `selectInput` choices): wrap the upload modal's `selectInput` choices in a closure that reads `shared$company_map()` at modal-open time, OR convert to `uiOutput` + `renderUI`.
- Line 353 (proveedor edit/create empresa filter): same treatment.

The simpler fix: since these are inside modal builders, just replace `names(COMPANY_MAP)` and `setNames(names(COMPANY_MAP), unname(COMPANY_MAP))` with the equivalent via `shared$company_map()`. Modals are rebuilt every time they're opened, so a fresh read is guaranteed.

**Acceptance:** Add a new empresa, then open the Proveedores Settings panel — the empresa appears in the dropdown.

### Task 3.5 — `data_pipeline.R::aggregate_ic_codes` reactive empresa list
**File:** `R/data_pipeline.R`
**Block:** line 332.
**Action:** This function is called from app.R / module servers. The caller has access to `shared$company_map()`. Pass the empresas vector in as a function argument:

```r
aggregate_ic_codes <- function(snapshots_ar, snapshots_ap, registry_v2,
                                empresa_initials = names(COMPANY_MAP)) {
  inv_map <- setNames(empresa_initials, ...)
  # ... rest unchanged, using empresa_initials instead of names(COMPANY_MAP) ...
}
```

Update callers to pass `empresa_initials = names(shared$company_map())`.

**Acceptance:** No regression in IC scanning. New empresas are scanned on the next IC refresh.

### Task 3.6 — `sap_api.R` accept empresa list as argument
**File:** `R/sap_api.R`

**Action:** `fetch_all_companies` and `sap_companies_available` should accept the empresa-initials vector as an argument with `names(COMPANY_MAP)` as the default. Update callers (search for `fetch_all_companies(` in `app.R` and elsewhere) to pass `names(shared$company_map())` instead of relying on the global.

**Acceptance:** SAP refresh after adding PL → the fetcher tries `SAP_PL_*` env vars (and skips if not configured, with the existing log message).

---

## Phase 4 — Asymmetric / inconsistent fixes

### Task 4.1 — Symmetric link/unlink in pagar_hoy
**File:** `R/pagar_hoy_module.R`
**Block:** `ph_unlink_supplier` observer at lines 1498–1520, specifically line 1510.
**Action:** Replace the unlink filter to scope by Empresa parallel to link's writer at line 1473–1475:

```r
      am <- am[!(toupper(trimws(am$Parte)) == toupper(parte) &
                 (am$Empresa == emp |
                  (emp == "" & (is.na(am$Empresa) | am$Empresa == "")))),
               , drop = FALSE]
```

**Acceptance:** Linking "FedEx Mex" → "FEDEXMX" for empresa NTS only, then unlinking from NTS, leaves the same Parte's link for empresa NCS untouched. Verify by:
1. From Settings → Proveedores, set up two test entries with the same alias for two different empresas.
2. Stage an AP row with a matching Parte under empresa NTS. Confirm green dot.
3. Click the alias link button to manually re-link to a different alias.
4. Stage same Parte under empresa NCS. Confirm its link is unchanged.

### Task 4.2 — `unstage_pagar_hoy` supports id-based unstage
**File:** `R/persistence.R`
**Function:** `unstage_pagar_hoy` at lines 528–532.
**Action:** Add an optional `keys` argument, parallel to `upsert_pagar_hoy`:

```r
unstage_pagar_hoy <- function(db, keys_df,
                              keys = c("ledger", "Empresa", "Moneda", "Documento")) {
  if (!nrow(keys_df)) return(db)
  dplyr::anti_join(db, keys_df, by = keys)
}
```

Update callers that intend id-based unstage (manual entries, abonos) to pass `keys = "id"`.

**Acceptance:** Unstaging by `id` removes only the specific row, not all rows sharing `(ledger, Empresa, Moneda, Documento)`.

### Task 4.3 — `to_calendar_data` includes Empresa in group key
**File:** `R/data_pipeline.R`
**Function:** `to_calendar_data` at line 388.
**Action:**
1. Add `Empresa` to `required_cols` (line 394).
2. Change the group key (line 418):
   ```r
       group_by(Empresa, Fecha, Moneda, Parte) |>
   ```
3. The result tibble's first column becomes `Empresa` — verify downstream consumers (`calendar_html`, calendar bubble click handlers) handle this. Specifically, `R/global.R::calendar_html` (around lines 317–500) consumes this output. The bubble per-day aggregation may need to additionally `summarise(... .by = c(Fecha, Moneda))` if it currently expects one row per (Fecha, Moneda, Parte). **Audit the consumers carefully** — this change has wider blast radius than it looks.

**Acceptance:** Multi-empresa selection: a single vendor "FedEx" appearing on the same day for both NTS and NCS shows as two distinct rows in the day modal's Parte breakdown (one per empresa), not collapsed into one row whose total comes from both.

### Task 4.4 — Defensive `load_pagar_hoy` for NA status
**File:** `R/persistence.R`
**Function:** `load_pagar_hoy` at lines 506–509.
**Action:**

```r
load_pagar_hoy <- function() {
  df <- .normalize(.s3_read(S3_KEYS$pagar_hoy), .schema_pagar_hoy)
  # Recover NA status as "pending" instead of silently dropping. NA happens
  # rarely (e.g. concurrent write race) but losing items invisibly is worse
  # than reviving stale ones.
  df$status[is.na(df$status) | !nzchar(trimws(df$status))] <- "pending"
  dplyr::filter(df, status %in% c("pending", "confirmed", "cancelled"))
}
```

**Acceptance:** A pagar_hoy row whose `status` was somehow nulled out reappears as `pending` instead of vanishing.

### Task 4.5 — `save_*` functions cache normalized df
**Files:** `R/persistence.R`, `R/bancos_persistence.R`
**Functions to fix:** `save_pagar_hoy` (line 511), `save_manual` (line 354), and any other `save_*` that calls `.cache_set` after `.s3_write(.normalize(df, ...))`.

**Action:** Compute the normalized version once, cache that:

```r
save_pagar_hoy <- function(df) {
  norm <- .normalize(df, .schema_pagar_hoy)
  .s3_write(norm, S3_KEYS$pagar_hoy)
  if (exists(".cache_set", mode = "function")) .cache_set("pagar_hoy", norm)
}

save_manual <- function(df) {
  norm <- .normalize(df, .schema_manual)
  .s3_write(norm, S3_KEYS$manual)
  if (exists(".cache_set", mode = "function")) .cache_set("manual", norm)
}
```

Apply the same pattern to every `save_*` function that has a `.cache_set` line.

**Acceptance:** Same-session reads from cache and S3-loaded reads produce structurally identical data frames.

---

## Phase 5 — Abono parcial wiring

This phase implements partial payments end-to-end per the user's spec:
- Add a third button in the Agregar picker.
- Selecting an invoice and an abono amount reduces the calendar's displayed balance for that invoice (already works).
- The abono amount is also staged to Agenda de Hoy as a pending row, distinct from any parent-invoice row.
- A hover popup on the calendar's day modal shows: original `Saldo_original`, list of all abonos against the invoice, current displayed balance.
- Special case: if the parent invoice is already in Agenda for today (status=pending), the abono *modifies* the existing pagar_hoy row's `Importe` instead of inserting a new row.

### Task 5.1 — Add a third "Abono parcial" button to the Agregar picker
**File:** `app.R`
**Block:** `observeEvent(input$btn_add_entry, ...)` at lines 1326–1335.
**Action:** Add a third action button:

```r
  observeEvent(input$btn_add_entry, {
    showModal(modalDialog(
      title = "¿Qué tipo de entrada?", size = "s", easyClose = TRUE,
      footer = tagList(
        modalButton("Cancelar"),
        actionButton("pick_ar", "Cobro \u2014 CxC", class = "btn btn-primary"),
        actionButton("pick_ap", "Pago \u2014 CxP",  class = "btn btn-success"),
        actionButton("pick_abono", "Abono parcial",  class = "btn btn-warning")
      )
    ))
  })

  observeEvent(input$pick_abono, {
    removeModal()
    show_abono_modal(sap_data, session)
  }, ignoreInit = TRUE)
```

**Acceptance:** Clicking "Agregar" → picker shows three buttons. Clicking "Abono parcial" → opens the existing abono modal.

### Task 5.2 — Add `tipo_item` column to pagar_hoy schema
**File:** `R/persistence.R`
**Function:** `.schema_pagar_hoy` at lines 491–504.
**Action:** Add `tipo_item` between `Codigo` and `Importe`:

```r
.schema_pagar_hoy <- function() tibble(
  id         = character(),
  ledger     = character(),
  Empresa    = character(),
  Moneda     = character(),
  Documento  = character(),
  Parte      = character(),
  Codigo     = character(),
  tipo_item  = character(),    # "factura" (default) | "abono" — distinguishes
                               # full invoice payments from partial-payment rows
  Importe    = numeric(),
  FechaVenc  = as.Date(character()),
  staged_by  = character(),
  staged_at  = as.POSIXct(character()),
  status     = character()
)
```

`.normalize` (`R/persistence.R:222–237`) handles backwards compatibility: existing rows without `tipo_item` get NA, which we'll treat as `"factura"`.

**Acceptance:** Loading an old pagar_hoy from S3 still works. Newly-staged rows include `tipo_item = "factura"` for invoice payments and `"abono"` for abonos.

### Task 5.3 — Update all existing pagar_hoy writers to set `tipo_item = "factura"`
**Files:** Every staging-row builder. Same files Phase 1 touched:
- `app.R:1425-1438` (manual entry agenda toggle)
- `R/staging_browse_module.R:309-324` (browse-by-range)
- `R/ledger_module.R:560-568, 606-614, 737-745, 812-826` (calendar day modal)
- `R/search_module.R:833-849` (search/vencidos handler)
- `R/bancos_module.R:2783-2796` (return to calendar)
- `R/interco_module.R:347-360` (IC stage)
- `R/treasury_map_module.R:824-838` (treasury map)

**Action:** In each `tibble::tibble(...)` or `data.frame(...)` row builder, add `tipo_item = "factura"` immediately after `Codigo`.

**Acceptance:** Every existing staging path produces rows with `tipo_item = "factura"`.

### Task 5.4 — Wire abono handler to stage to pagar_hoy
**File:** `R/staging_browse_module.R`
**Function:** `setup_abono_browse`, the `observeEvent(input$ab_rows, ...)` block at lines 547–585.
**Action:** Rewrite the handler. The new logic:

1. Save abono records (keep existing).
2. For each abono row, check whether a pagar_hoy row exists for `(ledger, Empresa, Moneda, Documento)` with `status="pending"` and `tipo_item="factura"`:
   - If yes → decrement that row's `Importe` by the abono amount. (Don't go below 0.)
   - If no → insert a new pagar_hoy row with `tipo_item="abono"`, `Importe = abono amount`, `FechaVenc = Sys.Date()`.
3. Save pagar_hoy.

```r
  observeEvent(input$ab_rows, {
    rows_data <- input$ab_rows
    if (!is.list(rows_data) || length(rows_data) == 0) return()

    ledger <- input$ab_ledger %||% "AR"

    # ── 1. Save abono records (calendar balance reduction) ────────────────────
    abono_rows_df <- dplyr::bind_rows(lapply(rows_data, function(r) {
      tibble::tibble(
        id          = uuid::UUIDgenerate(),
        ledger      = ledger,
        Empresa     = r$empresa    %||% "",
        Moneda      = r$moneda     %||% "MXN",
        Documento   = r$documento  %||% "",
        Parte       = r$parte      %||% "",
        importe     = as.numeric(r$importe %||% 0),
        fecha_abono = Sys.Date(),
        notas       = "",
        created_by  = current_user(),
        created_at  = Sys.time(),
        status      = "active"
      )
    }))
    ab_updated <- upsert_abono(abonos_db() %||% load_abonos(), abono_rows_df)
    abonos_db(ab_updated)
    tryCatch(save_abonos(ab_updated),
             error = function(e) showNotification(
               paste("No se pudo guardar el abono:", e$message), type = "warning"))

    # ── 2. Stage to pagar_hoy as `tipo_item = "abono"` (or modify existing
    #      pending parent row's Importe if present) ──────────────────────────
    ph <- pagar_hoy_db() %||% load_pagar_hoy()

    for (i in seq_len(nrow(abono_rows_df))) {
      ar <- abono_rows_df[i, ]
      r_raw <- rows_data[[i]]   # for codigo

      parent_mask <- ph$ledger == ledger &
                     ph$Empresa == ar$Empresa &
                     ph$Moneda  == ar$Moneda &
                     ph$Documento == ar$Documento &
                     ph$status == "pending" &
                     (is.na(ph$tipo_item) | ph$tipo_item == "factura")

      if (any(parent_mask)) {
        # Modify existing pending parent row's Importe
        idx <- which(parent_mask)[1]
        ph$Importe[idx] <- pmax(0, ph$Importe[idx] - ar$importe)
      } else {
        # Insert a fresh abono row
        new_row <- tibble::tibble(
          id        = uuid::UUIDgenerate(),
          ledger    = ledger,
          Empresa   = ar$Empresa,
          Moneda    = ar$Moneda,
          Documento = ar$Documento,
          Parte     = ar$Parte,
          Codigo    = trimws(as.character(r_raw$codigo %||% "")),
          tipo_item = "abono",
          Importe   = ar$importe,
          FechaVenc = Sys.Date(),
          staged_by = current_user(),
          staged_at = Sys.time(),
          status    = "pending"
        )
        ph <- upsert_pagar_hoy(ph, new_row, keys = "id")
      }
    }

    pagar_hoy_db(ph)
    tryCatch(save_pagar_hoy(ph),
             error = function(e) showNotification(
               paste("No se pudo guardar en Agenda:", e$message), type = "warning"))

    n <- nrow(abono_rows_df)
    showNotification(
      paste0(n, if (n == 1L) " abono registrado" else " abonos registrados",
             " y enviados a Agenda de hoy."),
      type = "message", duration = 3
    )
    removeModal()
  }, ignoreInit = TRUE)
```

**Acceptance:**
- Register a 30k abono on a 100k invoice → calendar shows 70k for that invoice → Agenda de Hoy shows a 30k abono row tagged distinctly.
- Stage the parent invoice for today first (showing 100k in Agenda), then register a 30k abono → Agenda's row for that invoice is now 70k, no separate abono row.
- Register a 50k abono and a 30k abono on the same 100k invoice in succession → calendar shows 20k → Agenda shows two abono rows (50k + 30k) OR one modified parent at 20k depending on whether the parent was staged.

### Task 5.5 — Visually distinguish abono rows in Agenda de Hoy
**File:** `R/pagar_hoy_module.R`
**Block:** AP table render at lines 567–694.
**Action:** In the `Proveedor` column case_when (around line 630–661), prepend an "Abono" badge for rows where `tipo_item == "abono"`. For example:

```r
            Proveedor = dplyr::case_when(
              ppl_status == "ok" ~ paste0(
                # ── Existing OK content ──
                if_else(coalesce(tipo_item, "factura") == "abono",
                        '<span class="badge bg-warning text-dark me-1" style="font-size:0.65em;">ABONO</span>',
                        ''),
                '<span class="ph-dot ph-dot-ok" title="Incluido en BanBajío"></span>',
                # ... rest as before ...
              ),
              # ... same prefix for no_clabe and TRUE branches ...
            )
```

(Add the badge prefix to all three branches consistently.)

**Acceptance:** Abono rows visually stand out in Agenda de Hoy.

### Task 5.6 — Hover popup on day modal showing original + abonos
**File:** `R/ledger_module.R`
**Block:** day modal detail rendering (search around `.detail_to_audit_tbl` at line 856 and the row HTML builder for the audit table).

**Action:** For invoices where `has_abono == TRUE`, add a `title` attribute (basic HTML tooltip) or a small ⓘ icon with a richer tooltip. The data needed is in `df_combined`:
- `Saldo_original` — the original Saldo vencido before abonos.
- `abono_total` — sum of active abonos.
- `Importe` — currently displayed (post-abono).

Plus the per-abono detail from `shared$abonos_db()`: filter by `(ledger, Empresa, Moneda, Documento)`, `status == "active"`, get `(fecha_abono, importe, notas)`.

Implementation hint: in the row HTML builder, add a span with bootstrap tooltip:

```r
if (isTRUE(row$has_abono)) {
  tooltip_html <- paste0(
    "Original: ", fmt_money(row$Saldo_original), " ", row$Moneda, "&#10;",
    "Abonos: ", fmt_money(row$abono_total), " ", row$Moneda, "&#10;",
    "Saldo: ", fmt_money(row$Importe), " ", row$Moneda
    # Extend with per-abono breakdown if shared$abonos_db is in scope
  )
  paste0('<span class="badge bg-warning text-dark" data-bs-toggle="tooltip" ',
         'title="', htmltools::htmlEscape(tooltip_html), '">⊖ Abono parcial</span>')
}
```

The simpler version (no Bootstrap tooltip): use a native `title=` attribute on the row; the browser renders it on hover.

**Acceptance:** Hovering over an invoice row that has abonos shows a popup with original amount, abonos total, and current displayed amount.

### Task 5.7 — Abono delete unstages from pagar_hoy
**File:** `R/staging_browse_module.R` (or wherever abono soft-delete is handled).
**Action:** When an abono's status flips from `"active"` to `"deleted"`:
1. Recompute the parent invoice's pagar_hoy row's `Importe` (if a pending one exists with `tipo_item="factura"`).
2. OR remove the corresponding `tipo_item="abono"` row from pagar_hoy.

Implementation depends on where abonos are deleted — search for `delete_abono` or `status.*deleted` in `R/persistence.R` and follow the call sites. If no UI for deleting abonos exists yet, this task is deferred (the data model just stays consistent because `active_abonos_summary` filters by `status == "active"`).

**Acceptance:** Deleting an abono → calendar balance restores → corresponding agenda row removed or parent's Importe restored.

---

## Phase 6 — Documentation / deployment checklist

### Task 6.1 — Add deployment checklist file
**Action:** Create `EMPRESA_DEPLOYMENT_CHECKLIST.md` in the project root with this content:

```markdown
# Deployment checklist for adding a new empresa to a group

When a customer needs to add a new company to their group, the following steps must all be completed for the empresa to be fully wired:

1. **Empresa registry**
   - Open the app → Grupo → Empresas → "Nueva empresa".
   - Fill in initials (e.g. "PL"), razón social, nombre corto, RFC, optional alternative names.
   - Save. The empresa appears in the topbar empresa toggle.

2. **SAP credentials (if SAP-integrated)**
   - On the deploy host, set environment variables:
     - `SAP_<INITIALS>_URL`
     - `SAP_<INITIALS>_COMPANY`
     - `SAP_<INITIALS>_USER`
     - `SAP_<INITIALS>_PASSWORD`
   - Restart the app process so env vars load.

3. **Bank accounts**
   - Configuración → Cuentas → "Nueva cuenta".
   - Add at least one cuenta with `Empresa = <INITIALS>`, valid CLABE/cuenta, currency, and `is_ppl_default = TRUE` for the currency you'll use for Bajío TXT.

4. **Intercompany registry**
   - Configuración → Intercompany.
   - In the new empresa's section, add the AR (clientes IC) and AP (proveedores IC) numeric CardCode bases for every sister-company relationship.
   - Save.

5. **Suppliers / clients catalog**
   - Configuración → Catálogo → "Nuevo proveedor" or via Excel upload.
   - For each supplier this empresa pays via Bajío TXT, ensure `alias`, `clabe`, `medio_pago` are populated.
   - You can scope a supplier to a specific empresa or leave Empresa blank for "all empresas."

6. **Verify**
   - Switch to AP / CxP → confirm calendar bubbles appear.
   - Click any day → confirm staging works.
   - Open Agenda de Hoy → confirm the empresa's tab is visible.
   - Generate a Bajío TXT → confirm output is valid.

If any step is skipped, the affected feature will appear partially broken (e.g. tab missing, sums wrong, TXT empty).
```

**Acceptance:** File exists and is readable.

---

## Order of execution & verification

Execute phases in numerical order. Each phase has dependencies on the previous:

1. **Phase 0 (cleanup)** — safe, no behavior change, makes the rest easier.
2. **Phase 1 (Codigo)** — establishes the data contract. Verify by checking that AP rows arriving in Agenda de Hoy from the calendar's day modal show green/yellow correctly when the supplier is in catalog.
3. **Phase 2 (manual entry bug)** — independent of Phase 1, can be done in parallel.
4. **Phase 3 (reactive companies)** — depends on Phase 0 (root global.R deletion). The most invasive task in the whole plan; touch carefully and test thoroughly with at least one new empresa added mid-session.
5. **Phase 4 (asymmetries)** — independent of others, can be done any time.
6. **Phase 5 (abono parcial)** — depends on Phase 1 (Codigo plumbing) and Phase 0 (cleanup).
7. **Phase 6 (docs)** — last.

After all phases:
- Run a manual end-to-end test: Add empresa "TEST" → verify it appears in topbar, calendar empresa toggle, Agenda tab, treasury map, IC table, proveedores dropdown.
- Manually create 5 manual entries with various Empresa/Moneda/Documento combinations, half with "send to agenda" toggle, half without. Verify all 5 appear on the correct calendar day, and the toggled ones appear in Agenda de Hoy.
- Stage 5 SAP invoices from a calendar day modal. Verify each ends up in Agenda de Hoy with green/yellow color (Codigo found in catalog).
- Generate a Bajío TXT. Validate against a known-good production file byte-by-byte if possible.
- Register an abono parcial. Verify calendar balance reduces, Agenda gains a row.

---

## Reference: file map

Files this plan touches (in order of first edit):

- `app_timed.R` (delete)
- `/global.R` (delete)
- `R/bootstrap_proveedores.R` (delete)
- `R/manual_entry_handlers.R` (edit — remove unused function)
- `R/data_pipeline.R` (edit — Codigo harmonization, IC code aggregator)
- `R/ledger_module.R` (edit — Codigo plumbing, bancos_confirmados key fix, defensive empresa filter)
- `R/vencidos_module.R` (edit — disp_df, DOM)
- `www/vencidos.js` (edit — venRowPayload)
- `R/search_module.R` (edit — keep list, DOM, JS payloads, handler)
- `R/bancos_module.R` (edit — Codigo, possibly schema for codigo lowercase)
- `R/bancos_persistence.R` (edit — possibly add codigo to bancos_confirmados schema)
- `R/interco_module.R` (edit — Codigo, upsert, reactive empresas)
- `R/treasury_map_module.R` (edit — Codigo, upsert, reactive empresas)
- `R/persistence.R` (edit — schema add tipo_item, unstage keys arg, NA status recovery, normalized cache)
- `R/pagar_hoy_module.R` (edit — reactive empresas, asymmetric unlink, abono badge)
- `R/proveedores_module.R` (edit — reactive empresas)
- `R/sap_api.R` (edit — accept empresas as argument)
- `R/staging_browse_module.R` (edit — abono → pagar_hoy)
- `app.R` (edit — pick_abono observer, empresa_sel preservation, tipo_item in manual writer)
- `EMPRESA_DEPLOYMENT_CHECKLIST.md` (create)
