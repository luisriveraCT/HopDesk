# R environment & secrets — never commit
.Renviron
.Rhistory
.RData
**/.Rhistory
**/.RData

# R temp files
.Rproj.user/

# Docker env files — contain AWS credentials and API keys
.env
.env.*
.env.*.local

# One-time ephemeral scripts (may contain inline credentials)
scripts/register_*.R
scripts/seed_*.R

# OS
.DS_Store
Thumbs.db
